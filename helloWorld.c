#include <stdio.h> // preprocessor statement to load header file

// main() is program entry point and must be present
int main() { 
  char invite[] = "Let\'s write some C";
  printf("Hello, %s!", invite); // formatted printing 
}
